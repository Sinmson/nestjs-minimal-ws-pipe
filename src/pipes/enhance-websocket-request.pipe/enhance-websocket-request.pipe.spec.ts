import { EnhanceWebsocketRequestPipe } from './enhance-websocket-request.pipe';

describe('EnhanceWebsocketRequestPipe', () => {
  it('should be defined', () => {
    expect(new EnhanceWebsocketRequestPipe()).toBeDefined();
  });
});
